fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic-settings==2.7.0
beautifulsoup4==4.12.3
lxml==6.1.3
lxml_html_clean==0.4.5
readability-lxml==0.8.1
python-dateutil==2.9.0.post0

# Security floors for deps flagged by pip-audit / Trivy (all have fixes). Pinned
# so the resolved image can't regress below the fix; this mirrors the block in
# apps/api/requirements.txt, and the fastapi==0.139.0 + starlette==1.3.1 pair is
# the one apps/api already runs. (#314)
#   starlette       < 1.3.1 — PYSEC-2026-161 / -248 / -249 / -2280 / -2281, incl.
#                             CVE-2026-48818 (StaticFiles SSRF + NTLM credential
#                             theft via UNC paths) and CVE-2026-54283
#                             (request.form() limits ignored for urlencoded
#                             bodies → DoS). fastapi==0.125.0 capped starlette at
#                             <0.51.0, hence the fastapi bump alongside.
#   lxml_html_clean < 0.4.5 — PYSEC-2026-2614 / CVE-2026-49825: Cleaner URL
#                             bypass via missing xlink:href handling. Relevant
#                             here — the importer sanitizes fetched recipe HTML.
#                             0.4.5 requires lxml>=6.1.1, hence the lxml bump.
